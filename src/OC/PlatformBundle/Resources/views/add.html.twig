{% extends "PlatformBundle::layout.html.twig" %}

{% block title %}
    Modifier une annonce - {{ parent() }}
{% endblock %}

{% block oc_platform_body %}

    <h2>Ajouter une annonce</h2>

    {{ include("PlatformBundle::form.html.twig") }}

    <p>
        Vous souhaitez ajouter une annonce ? Un peu de patience !
    </p>

    <p>
        <a href="{{ path('home') }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour à la page d'accueil
        </a>
    </p>

{% endblock %}